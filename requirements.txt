streamlit
# 1. 核心修复：锁定 Numpy 版本，防止编译失败
numpy<2.0.0

# 2. 核心修复：使用 headless OpenCV，体积小且不依赖图形界面
opencv-python-headless

# 3. 核心库：锁定 Paddle 版本，防止下载最新版过大
paddlepaddle==2.6.1
paddleocr>=2.7.0

# 4. 关键依赖：锁定 protobuf 避免与 Streamlit 冲突
protobuf==3.20.3

# 5. 业务库
python-docx
